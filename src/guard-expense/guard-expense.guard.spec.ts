import * as guardExpenseGuard from './guard-expense.guard';

describe('GuardExpenseGuard', () => {
  it('should be defined', () => {
    expect(guardExpenseGuard.SimpleAuthGuard).toBeDefined();
  
  })
});
